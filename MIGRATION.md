# MIGRATION — مسار الهجرة من React PWA إلى Flutter (ADR-001)

> **قاعدة حاكمة:** الهجرة مرحلية. الـPWA الحالية هي المنتج الحي ولا تُهدم؛
> الموقع المنشور على GitHub Pages وworkflow بناء الـAPK وapp id
> (`app.falah.studio`) يبقون يعملون في كل لحظة. **لا تُنفَّذ أي خطوة من هذا
> الملف إلا بقرار صريح يبدأ المرحلة M1.**

## ما يُنقل كما هو (طبقة مشتركة — صفر إعادة بناء)

| الأصل | لماذا لا يتغير |
|---|---|
| `supabase/migrations/` (المخطط + RLS) | خلفية مشتركة؛ Flutter يتصل بنفس القاعدة |
| Edge Functions (`ai-assistant`، النشر) | الأسرار والمنطق الخادمي مستقلان عن الواجهة |
| بيانات القرآن والحديث الموثقة وبصماتها | المصدر والـchecksums لا علاقة لهما بالإطار |
| قواعد SOURCE_LOCK (خط التحقق، حالات المراجعة) | سياسة منتج، تُعاد كتابتها بنفس الخوارزميات في Dart مع نقل اختباراتها |
| التوثيق وADRs وworkflows الـCI المفاهيمية | تُحدَّث لا تُستبدل |

## ما يُعاد بناؤه في Flutter (بالـPWA مرجعًا للتصميم والسلوك)

| ميزة الـPWA (المرجع) | مقابل Flutter |
|---|---|
| `core/design` (Design Tokens بـ CSS vars) | `lib/core/theme/tokens.dart` بنفس القيم |
| `core/i18n` (ar/en + سجل لغات) | `lib/core/l10n` (ARB) بنفس المفاتيح |
| `core/sourcelock` + اختباراته | `lib/core/sourcelock` — **تُنقل الاختبارات أولًا** (اختبارات القفل هي عقد الهجرة) |
| محرر Canvas (`renderEngine.ts`) | `CustomPainter` بنفس نموذج العناصر (JSON المشاريع الحالي يبقى صالحًا) |
| الفيديو (معاينة الجهاز) | معاينة Flutter + Worker خلفي (ADR-004) |
| الشاشات (خرائط التنقل §12) | نفس بنية التبويبات والـDrawer |

## ما يُستبدل نهائيًا

- Zustand → Riverpod (ADR-003)، Dexie/IndexedDB → Drift/Isar (يُحسم بـADR عند M1)
  مع مهاجر بيانات يقرأ صيغة JSON الحالية للمشاريع.
- Service Worker/PWA packaging → توزيع Flutter Web + متاجر التطبيقات.

## مراحل التنفيذ (عند اتخاذ قرار البدء)

- **M0 — تجهيز (لا يمس الإنتاج): ✅ أُنجزت 2026-09-04.** `flutter_app/` (Flutter 3.35.4)،
  ‏Tokens وi18n (359 مفتاح ARB) وsourcelock+contextValidation منقولة، بوابة الخروج
  مجتازة: 12/12 اختبار Dart أخضر + `flutter analyze` صفر مشاكل + CI مستقل `flutter.yml`.
- **M1 — القراءة:** قرآن/حديث/مكتبة للقراءة فقط متصلة بنفس Supabase.
- **M2 — الإنشاء:** المحرر والقوالب وتصدير الصور (تكافؤ مع الـPWA).
- **M3 — الفيديو والنشر:** معاينة + Worker (ADR-004)، الجدولة، Connectors.
- **M4 — التبديل:** iOS TestFlight، Android يستبدل غلاف Capacitor **بنفس app id**،
  وFlutter Web يحل محل الـPWA على نفس الرابط. الـPWA تُؤرشف في فرع مرجعي ولا تُحذف.

كل مرحلة تخضع لمعايير القبول (§36) قبل بدء التالية، والتقدم يُدوَّن في `PROJECT_STATUS.md`.
