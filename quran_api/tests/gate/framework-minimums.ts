/**
 * The per-category minimum number of executed cases, in one place so the gate
 * runner, the merge tool and the release report all read the same numbers.
 */
export { MINIMUMS } from './report.ts';
