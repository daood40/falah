name: falah_hadith_api
description: Dart client for the FALAH Hadith API — models, HTTP client and repository.
version: 1.0.0
publish_to: 'none'

environment:
  sdk: ^3.5.0

dependencies:
  http: ^1.2.2
