# memory/PROJECT_MEMORY.md   (آخر تحديث: 2026-09-16)

## ما هو
فلاح — منصة لصانع المحتوى الإسلامي: تصميم منشورات آيات وأحاديث موثقة،
قراءة القرآن دون إنترنت، سبحة وأذكار. القاعدة الحاكمة SOURCE_LOCK:
لا يُؤلَّف أو يُعدَّل نص شرعي أبدًا.

## المكدّس
- المنتج الحي: React 18 + TS strict + Vite PWA (v2.2.0) + غلاف Capacitor
  لأندرويد (app.falah.studio). ينشر من main إلى GitHub Pages.
- الهجرة الجارية: Flutter 3.35.4 في `flutter_app/` (Riverpod + go_router +
  shared_preferences + crypto). الـPWA تبقى المنتج الحي حتى M4 (MIGRATION.md).
- الخلفية: Supabase (مخطط 34 جدولًا جاهز) — غير مفعّلة، تنتظر مفاتيح المالك.
- خادم بيانات الحديث: `hadith-api/` (Node 22 + pg، بلا إطار) فوق مخطط `corpus`
  في نفس Supabase — 15 جدولًا، 26 نقطة نهاية، 103 اختبارًا. بلا نص مستورد بعد.

## الحالة اليوم (2026-09-09)
- يعمل: الـPWA كاملة (قرآن/حديث/محرر/فيديو/مكتبة/جدولة/سبحة/أذكار/ورد)،
  وتطبيق Flutter بشاشاته الست (رئيسية/سور/قارئ/سبحة+صيغ ذكر/أذكار/إعدادات).
- لا يعمل: المزامنة والحسابات (لا مفاتيح Supabase بعد)؛ iOS مؤجل (لا حساب Apple).

## أين توقّفنا
2026-09-09: دُمج PR #18 — حزمة نشر Google Play كاملة (توقيع + workflow AAB
+ privacy.html + RELEASE_PLAY.md). المتبقي على المالك: حساب Play + مفتاح
التوقيع والأسرار الأربعة، ثم تشغيل «Android Release (AAB)» ورفع الناتج.
الخطوة التقنية التالية: M2 المحرر في Flutter حسب MIGRATION.md، أو ربط
Supabase عند وصول المفاتيح.

2026-09-16: بُني Backend + REST API لكتب الحديث (`hadith-api/`, هجرات 0003–0005) مع
مستورد متعدّد المحوّلات وتوثيق OpenAPI وتكامل Flutter. كل الاختبارات خضراء على
PostgreSQL حقيقي، والـAPI شُغّل فعليًا. المتبقي: ملف مصدر «الجامع الكامل»
المصرَّح باستخدامه (`hadith-api/contracts/DATA_CONTRACT.md`) وتأكيد الحقوق قبل
رفع `CONTENT_LICENSE_CONFIRMED` إلى true.

2026-09-16 (تدقيق نهائي): شُغّل كل شيء من جديد بدل الاعتماد على التقرير السابق. نُصّب
Flutter SDK 3.35.4 في الجلسة وشُغّلت اختباراته فعليًا (36 + 8 اختبار عقد حيّ مقابل
الـAPI)، فظهر خطآن حقيقيان وأُصلحا (شاشة الحديث لم تكن تُترجم، واختبارات العميل
كانت تُرسل عربيًا بترميز Latin-1). أُضيفت عيّنة التحقق البشرية
(`corpus.verification_samples` + 3 نقاط نهاية) وقواعد سلامة البيانات صارت 21 قاعدة
تُختبَر بزرع مخالفة لكل قاعدة. المحجوب كما هو: استيراد حقيقي، نشر، APK/iOS،
تحقق بشري، وترخيص المحتوى. التقارير في `hadith-api/reports/`.

2026-09-16 (استيراد الطبعة): وصلت 12 ملفًا من صاحب المشروع (تصدير الشاملة للطبعة).
كُتب محوّل `jami_kamil_shamela` ثم استُوردت الطبعة كاملة في 46 ثانية:
15,961 حديثًا، 0 فاشل، 0 مكرر، و29,146 رابط تخريج لـ26 كتابًا. تحقّق آلي لكل
سجل مقابل ملف المصدر (نص حرفي + صفحة + بصمة) = 15961/15961. ثلاثة أخطاء حقيقية
ظهرت بالتشغيل وأُصلحت: انزياح رقم الصفحة عند فاصل صفحة داخل الحديث، وترتيب
الكتب (كانت كلها 1)، وبطء البحث (2.4s ← 0.2s باتحاد مفهرس). المحجوب: النشر،
APK/iOS، التحقق البشري، والترخيص.

2026-09-17 (الاستقلال والمقارنة): فُصلت الخدمة إلى `hadith-api/` بهجراتها الثماني
وحاوياتها وعميلَيها (TypeScript بلا تبعيات، وحزمة Dart يستهلكها تطبيق Flutter
نفسه بدل نسخة مكرّرة). أُضيفت طبقة تحقق ثالثة: مقارنة كل السجلات بمجموعة منشورة
مستقلة (46,906 حديثًا) — 67.53% مؤيَّد و18.75% جزئي، و1,638 سجلًا بلا تأييد
ذهبت إلى طابور مراجعة بشرية بلا تعديل حرف. وأُضيفت موارد تصنيف مستقلة:
التخريج (26 كتابًا) والدرجات والمجلدات والفهرس الشجري.
