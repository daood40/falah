# memory/PROJECT_MEMORY.md   (آخر تحديث: 2026-09-17)

## ما هو
فلاح — منصة لصانع المحتوى الإسلامي: تصميم منشورات آيات وأحاديث موثقة،
قراءة القرآن دون إنترنت، سبحة وأذكار. القاعدة الحاكمة SOURCE_LOCK:
لا يُؤلَّف أو يُعدَّل نص شرعي أبدًا.

## المكدّس
- المنتج الحي: React 18 + TS strict + Vite PWA (v2.2.0) + غلاف Capacitor
  لأندرويد (app.falah.studio). ينشر من main إلى GitHub Pages.
- الهجرة الجارية: Flutter 3.35.4 في `flutter_app/` (Riverpod + go_router +
  shared_preferences + crypto). الـPWA تبقى المنتج الحي حتى M4 (MIGRATION.md).
- الخلفية: Supabase (مخطط 34 جدولًا جاهز) — غير مفعّلة، تنتظر مفاتيح المالك.
- خادم بيانات الحديث: `hadith_api/` (Node 22 + pg، بلا إطار) فوق مخطط `corpus`
  في نفس Supabase — 15 جدولًا، 26 نقطة نهاية، 103 اختبارًا. بلا نص مستورد بعد.

## الحالة اليوم (2026-09-09)
- يعمل: الـPWA كاملة (قرآن/حديث/محرر/فيديو/مكتبة/جدولة/سبحة/أذكار/ورد)،
  وتطبيق Flutter بشاشاته الست (رئيسية/سور/قارئ/سبحة+صيغ ذكر/أذكار/إعدادات).
- لا يعمل: المزامنة والحسابات (لا مفاتيح Supabase بعد)؛ iOS مؤجل (لا حساب Apple).

## أين توقّفنا
2026-09-09: دُمج PR #18 — حزمة نشر Google Play كاملة (توقيع + workflow AAB
+ privacy.html + RELEASE_PLAY.md). المتبقي على المالك: حساب Play + مفتاح
التوقيع والأسرار الأربعة، ثم تشغيل «Android Release (AAB)» ورفع الناتج.
الخطوة التقنية التالية: M2 المحرر في Flutter حسب MIGRATION.md، أو ربط
Supabase عند وصول المفاتيح.

2026-09-16: بُني Backend + REST API لكتب الحديث (`hadith_api/`, هجرات 0003–0005) مع
مستورد متعدّد المحوّلات وتوثيق OpenAPI وتكامل Flutter. كل الاختبارات خضراء على
PostgreSQL حقيقي، والـAPI شُغّل فعليًا. المتبقي: ملف مصدر «الجامع الكامل»
المصرَّح باستخدامه (`hadith_api/contracts/DATA_CONTRACT.md`) وتأكيد الحقوق قبل
رفع `CONTENT_LICENSE_CONFIRMED` إلى true.

2026-09-16 (تدقيق نهائي): شُغّل كل شيء من جديد بدل الاعتماد على التقرير السابق. نُصّب
Flutter SDK 3.35.4 في الجلسة وشُغّلت اختباراته فعليًا (36 + 8 اختبار عقد حيّ مقابل
الـAPI)، فظهر خطآن حقيقيان وأُصلحا (شاشة الحديث لم تكن تُترجم، واختبارات العميل
كانت تُرسل عربيًا بترميز Latin-1). أُضيفت عيّنة التحقق البشرية
(`corpus.verification_samples` + 3 نقاط نهاية) وقواعد سلامة البيانات صارت 21 قاعدة
تُختبَر بزرع مخالفة لكل قاعدة. المحجوب كما هو: استيراد حقيقي، نشر، APK/iOS،
تحقق بشري، وترخيص المحتوى. التقارير في `hadith_api/reports/`.

2026-09-16 (استيراد الطبعة): وصلت 12 ملفًا من صاحب المشروع (تصدير الشاملة للطبعة).
كُتب محوّل `jami_kamil_shamela` ثم استُوردت الطبعة كاملة في 46 ثانية:
15,961 حديثًا، 0 فاشل، 0 مكرر، و29,146 رابط تخريج لـ26 كتابًا. تحقّق آلي لكل
سجل مقابل ملف المصدر (نص حرفي + صفحة + بصمة) = 15961/15961. ثلاثة أخطاء حقيقية
ظهرت بالتشغيل وأُصلحت: انزياح رقم الصفحة عند فاصل صفحة داخل الحديث، وترتيب
الكتب (كانت كلها 1)، وبطء البحث (2.4s ← 0.2s باتحاد مفهرس). المحجوب: النشر،
APK/iOS، التحقق البشري، والترخيص.

2026-09-17 (الاستقلال والمقارنة): فُصلت الخدمة إلى `hadith_api/` بهجراتها الثماني
وحاوياتها وعميلَيها (TypeScript بلا تبعيات، وحزمة Dart يستهلكها تطبيق Flutter
نفسه بدل نسخة مكرّرة). أُضيفت طبقة تحقق ثالثة: مقارنة كل السجلات بمجموعة منشورة
مستقلة (46,906 حديثًا) — 67.53% مؤيَّد و18.75% جزئي، و1,638 سجلًا بلا تأييد
ذهبت إلى طابور مراجعة بشرية بلا تعديل حرف. وأُضيفت موارد تصنيف مستقلة:
التخريج (26 كتابًا) والدرجات والمجلدات والفهرس الشجري.

2026-09-17 (عقد Falah): أُعيد تشكيل الاستجابة للعقد العملي — قائمة مختصرة وتفصيل
قياسي مع `?include=` للأجزاء الثقيلة، وأضيفت `random` و`daily` (حتمي بالتاريخ
وبصمة المجموعة) وأجزاء الحديث الخمسة و`/version` و`/datasets` و`/sources/{id}`
و`/books/{id}/chapters`. عميل Dart صار رسميًا بنماذج مكتوبة الأنواع و24 دالة.
مسار Falah كاملًا مُختبَر حيًّا: 16 اختبارًا عبر HTTP بلا mock. وأُصلح خطأ حقيقي:
2,212 نسبة تخريج خاطئة (أسماء كتب وردت في السند مثل «أنس بن مالك» و«عن الحميدي»)
حُذفت بقاعدة استشهاد تميّز موضع الاستشهاد من موضع السند — فارتفع تأييد التخريج
من 75.30% إلى 77.01%.

2026-09-17 (التسليم المستقل): نُقل المجلد إلى `hadith_api/` استعدادًا لفصله كمستودع.
أُضيف تدقيق الطبعة (مانيفست 12 مجلدًا ببصماتها ومطابقتها بقاعدة البيانات)،
وسلسلة سلامة من أربع محطات (RAW→DB→API→EXPORT = 400/400)، وتصدير بمانيفست،
وتقرير تكرارات (130 مجموعة، بلا حذف)، وفحص أمني ستّي، واختبار نسخ واسترجاع
فعلي (البصمة نفسها بعد الاسترجاع)، وبوابة محتوى ثانية `PUBLIC_DATA_ENABLED`،
وكاش عميل يرفض ما لا تطابق بصمته، وحارس يمنع عنوان تطوير في بناء إنتاجي.
وثائق: API_USAGE (7 لغات) · SECURITY · LICENSE · CHANGELOG. خمسة تقارير في
`hadith_api/reports/`. المحجوب: الترخيص، النشر، Docker (لا daemon هنا)،
والتحقق البشري.

2026-09-17 (تدقيق ما قبل الإطلاق — 274,881 فحصًا): بُنيت منظومة تدقيق في
`hadith_api/src/audit/` بسبع عائلات (بيانات، قاعدة، API، أمن، مشروع/عقد،
Flutter، حزمة الاختبارات)، وكل فحص أمرٌ مستقل على كائن مختلف، والمعرّف المكرّر
نفسه يُسجَّل فشلًا حتى لا يتضخّم العدد. نُشرت بيئة Staging حقيقية (قاعدة مستعادة
من نسخة احتياطية + خادم بعملية ومنفذ مستقلين) واختُبر الـAPI من خارج الخادم عبر
HTTP. النتيجة النهائية: 274,881 فحصًا، 0 فشل، 2 محجوبان (لا Docker daemon ولا
Flutter SDK هنا — يعملان في CI)، 204 اختبار وحدة/تكامل خضراء.

ثمانية عيوب حقيقية ظهرت وأُصلحت: سطران زخرفيان («• • •») كانا محفوظين كحديثين
بلا نص ← إصدار تصحيحي `JAMI-KAMIL-1437-V2` (15,959 سجلًا، بصمة ab6bf4d5…)
مستورد من نفس الملفات ببصماتها، وV1 محفوظ `superseded`؛ ترميز نسبة مئوية فاسد
وحرف تحكم وعدد خارج مدى int4 كانت ترجع 500 ← صارت 400/422؛ العرض العام
`hadiths_public` كان غير مقروء لدور anon؛ البحث على كلمة شائعة 3.0s ← 0.46s
بعمود مولَّد مفهرس وعدّ في مرور واحد؛ وثغرات تغطية في عميلَي Dart/TypeScript؛
والتدقيق نفسه صار بوابة CI. لم يُمَس حرف واحد من نص أي حديث.
المحجوب كما هو: الترخيص المكتوب، النشر العام، Docker daemon، والتحقق البشري.

2026-09-17 (تكامل Falah مع الـAPI): وُصِّل تطبيق Flutter بالـAPI كطبقة بيانات
وحيدة: تبويب «الحديث» بخمس شاشات (كتب ← أبواب ← قائمة ← تفصيل ← بحث)، ترقيم
تراكمي بزر «تحميل المزيد»، كاش على shared_preferences يحكمه dataset_version
وcontent_hash، ورسائل خطأ مشتقة من رمز الخطأ لا رسالة عامة. الإعداد كله من
‎--dart-define‎ (عنوان، مهلة، حجم صفحة) فالانتقال staging↔production بلا تعديل كود.

نُصِّب Flutter 3.35.4 في الجلسة وشُغِّلت الاختبارات فعليًا: 85 اختبارًا خضراء
منها 21 اختبار تكامل يركّب الشاشات الحقيقية ويحادث الخادم عبر HTTP، ثم قيادة
التطبيق المبني في متصفح Chromium عبر Playwright: 10/10 خطوات مع لقطات في
‎flutter_app/build/e2e‎.

عيب حقيقي ظهر بالاختبار وأُصلح: Riverpod 3 يعيد المحاولة تلقائيًا ويُبقي الحالة
«تحميل» مع إرفاق الخطأ، فشاشة الخطأ لم تكن تظهر أبدًا عند انقطاع الخادم —
أُوقفت إعادة المحاولة التلقائية وصار الخطأ يسبق التحميل في العرض (hadithAsync).
المحجوب: الترخيص والنشر العام (النص ما زال محجوبًا في كل الشاشات).
