# memory/BUGS.md   (المفتوحة فقط — آخر تحديث: 2026-09-17)

لا أعطال مفتوحة معروفة.

آخر جولة: بوابة الجودة 2026-09-17 (39,413 حالة) كشفت ستة عيوب وأُغلقت كلها
في نفس الجلسة مع حارس انحدار لكل واحد:
- بحث `exact=true` كان يعيد 500 (بارامتر مربوط غير مُستخدم) — search.ts
- ترميز نسبة فاسد في المسار كان يعيد 500 — router.ts (الآن 422)
- بايت NUL في المسار/الاستعلام كان يعيد 500 — router.ts + app.ts (الآن 422)
- جسم طلب ضخم كان يقطع الاتصال بدل الرد — middleware.ts (تفريغ ثم 400)
- import_runs/audit_logs: RLS مفروضة بلا سياسة تمنع service_role — هجرة 007
- حالة جلسة متسرّبة على اتصال مجمّع تُسقط الطلب التالي — pool.ts (مهل محلية)

الاختبارات: 104/104 (API) · 75/75 (PWA) · Flutter محجوبة (لا SDK هنا).
