# تقرير جاهزية الإنتاج — بوابات qa-production-readiness

- التطبيق: فلاح (PWA v2.2.0 + غلاف Capacitor ‏app.falah.studio)
- الفرع: claude/falah-islamic-content-platform-hi3jhz · الأساس: ‏main ‏2ae8c82
- التاريخ: 2026-09-09 · المُختبِر: وكيل Claude (جولة عدائية qa-engineer-mode)
- البيئة: بناء إنتاج `npm run build` مُخدَّم محليًا + CI

## النتيجة: **NO-GO للإنتاج الآن** — البوابات: 17 | PASS ‏11 | FAIL ‏2 | NOT RUN ‏3 | BLOCKED ‏1

القرار متوقع ومطابق للواقع: بوابات الجهاز الحقيقي تكتمل حصرًا عبر مسار
Internal testing الذي تفرضه Google أصلًا على الحساب الجديد، وبوابتا
الأعطال والإيقاف قرارا منتج مسجلان أدناه.

| # | البوابة | الحالة | الدليل / السبب |
|---|---|---|---|
| 1 | البناء | PASS | ‏CI أخضر على ‏2ae8c82 (CI ‏#88، APK ‏#51) + بناء محلي اليوم |
| 2 | التحليل الساكن | PASS | ‏eslint ‏0 · ‏tsc ‏0 · ‏flutter analyze ‏0 (تشغيل 2026-09-09) |
| 3 | اختبارات الوحدة | PASS | ‏75/75 ‏(vitest) + ‏20 ‏Dart — تشغيل اليوم |
| 4 | اختبارات الواجهة | PASS | ‏9 ‏widget ‏Flutter + ‏sweep ‏12 مسارًا بنقر كل عنصر، صفر أخطاء console |
| 5 | التكامل (رحلة كاملة) | PASS | جولة عدائية اليوم: بحث بمدخلات خبيثة (2:999، ‏XSS، ‏رموز) بلا انهيار؛ إعادة تحميل مسار عميق تعمل؛ لا رحلة دخول (لا حسابات في هذا الوضع) |
| 6 | اختبارات API | PASS | سطح API صفر في هذا الإصدار: لا خادم مفعّل، الحزمة بلا استدعاءات شبكة إلا مشاركة يدوية — تُعاد البوابة كاملة عند تفعيل Supabase |
| 7 | الفحص الأمني | PASS | تاريخ git نظيف من القيم السرية (التطابقات ذكرٌ للكلمة في وثائق)؛ الحزمة بلا JWT؛ لا debuggable؛ إذن واحد؛ SVG محظور في الرفع |
| 8 | الأداء | NOT RUN | القياس الصادق يتطلب جهازًا/شبكة حقيقية؛ المتوفر: حزم gzip ‏(أكبر ملف 130KB) وتحميل محلي فوري — يُقاس في Internal testing |
| 9 | قاعدة البيانات | PASS | ‏`npm run db:validate` اليوم: schema valid + «rls behavioral: pass» على Postgres حقيقي |
| 10 | الشبكة | PASS | إيقاف الاتصال ثم إعادة تحميل → ‏Service Worker يقدّم التطبيق مع لافتة «غير متصل» (مُجرَّب اليوم) |
| 11 | إعدادات الإنتاج | PASS | لا localhost/‏10.0.2.2 في src؛ لا debuggable؛ الوجهة Pages الإنتاجية |
| 12 | الأسرار | PASS | فحص التاريخ + الملفات المتتبعة + الحزمة المبنية: نظيف؛ ‏jks/key.properties في ‏.gitignore |
| 13 | حزمة release على جهاز حقيقي | NOT RUN | لا جهاز في بيئة الوكيل — تُنفَّذ أول ما يُثبّت المالك AAB من Internal testing (خطوات RELEASE_PLAY.md) |
| 14 | تقارير الأعطال | FAIL | لا تقارير عن بُعد — يوجد سجل أخطاء محلي فقط (‏Settings). قرار منتج معلّق: ‏Sentry/Crashlytics يناقض «لا تتبع» ويغيّر نموذج سلامة البيانات — القرار للمالك (بند TODO) |
| 15 | النسخ الاحتياطي | PASS | لا خادم = لا قاعدة تُستعاد؛ تصدير/استيراد بيانات المستخدم داخل التطبيق موجود ومغطى في sweep — تُعاد البوابة بمعناها الكامل مع Supabase |
| 16 | مفتاح إيقاف عن بُعد | FAIL | غير موجود (يتطلب خادم إعدادات). مقبول مؤقتًا لأن التطبيق محلي بلا مدفوعات؛ إلزامي ضمن مرحلة تفعيل Supabase (بند TODO) |
| 17 | الخصوصية ونموذج البيانات | PASS | ‏privacy.html منشورة مع الموقع، مطابقة للجمع الفعلي (صفر)، وإجابات النموذج مكتوبة في RELEASE_PLAY.md |
| — | ‏(BLOCKED واحد ضمن 13) | BLOCKED | متطلب Google للحساب الجديد: اختبار مغلق 12 مختبِرًا/14 يومًا قبل الإنتاج — خارج المنتج، يُرفع بإتمام خطوة المالك |

## أعطال الجولة العدائية
1. [بسيط — أُصلح في هذا الـcommit] التاريخ الهجري كان يظهر بالعربية في الوضع
   الإنجليزي → ‏hijriToday أصبحت تراعي اللغة (مؤكد: «Rabiʻ I 27, 1448 AH»).
2. لا أعطال أخرى: مدخلات خبيثة، 320px بلا تمرير أفقي، إعادة تحميل عميقة، ‏offline — نظيفة.
   (النص القرآني يبقى عربيًا في وضع EN عمدًا — SOURCE_LOCK.)

## المسار إلى GO
1. المالك: حساب Play + مفتاح + أسرار → AAB → ‏Internal testing (يغلق 8 و13).
2. قرار المالك في 14 (تقارير أعطال مقابل «لا تتبع») و16 يُبنى مع Supabase.
3. عند تفعيل Supabase: إعادة البوابات 6 و15 و16 كاملة + تحديث نموذج البيانات.

مالك قرار الإطلاق: داوود — الوكيل لا يملك القرار.
