# SKILL_REGISTRY — سجل قدرات مساعد AI المقيّد

المرجع: v2 §17 وADR-005. المساعد **ليس مفتيًا** ولا يؤلف نصًا شرعيًا؛ كل قدراته إما
أدوات بحث ترجع سجلات موثقة بمعرّفاتها، أو مساعدة غير شرعية (تصميم/تنظيم/صياغة).

## الأدوات المعتمدة (v2) وحالتها في الكود الحالي

| الأداة | الوظيفة | الحالة اليوم (PWA) |
|---|---|---|
| `search_quran` | بحث في الآيات المعتمدة، يرجع `{surah, ayah, source_id, checksum}` | ✅ منفَّذة client-side: `findVerifiedReferences` → `searchQuran` (`features/ai/domain/assistant.ts`) |
| `search_hadith` | بحث في الأحاديث المعتمدة بدرجاتها المنسوبة | ✅ منفَّذة client-side عبر `searchHadiths` |
| `get_tafsir` | جلب تفسير آية من مصدر معتمد | 🟡 جزئي: إثراء التفسير في محرك القرآن؛ غير معروض كأداة للمساعد بعد |
| `get_translation` | جلب ترجمة معتمدة | 🟡 كما أعلاه (صحيح إنترناشونال متوفرة في المحرك) |
| `suggest_layout` | اقتراح قالب/ألوان/تخطيط (غير شرعي) | ✅ قواعد محلية في `LocalAssistantProvider` |

> **الفجوة المتبقية (ADR-005 طبقة 3):** نقل هذه الأدوات إلى Tool Calling داخل دالة
> `ai-assistant` مع Output Guard على الخادم، وتسجيل كل نداء في `ai_requests/ai_logs`.

## ما يُمنع تسجيله كقدرة — نهائيًا (v2 §7، §17)

فتوى أو حكم شرعي، تأليف/تعديل/ترجمة نص شرعي، تخمين درجة حديث، تلخيص محتوى ديني.
نصوص الرفض الموحدة (الملحق هـ) مطبقة في `features/ai/data/aiProvider.ts`.

## قواعد إضافة قدرة جديدة

1. تصنَّف أولًا: شرعية (تمر حصريًا عبر سجلات معتمدة بمعرّفات) أم غير شرعية.
2. تُضاف صفًا هنا مع موقع تنفيذها، وتُغطى باختبار (أحمر واحد على الأقل إن مست محتوى شرعيًا).
3. لا قدرة تُفعَّل بمفاتيح في العميل؛ الأسرار خادمية فقط (الملحق ب).
